name: My First Workflow

on: 
  push:
    branches: [ main ]

jobs:
  first_job:
    runs-on: ubuntu-latest

    steps:
      - name: This is the first step
        run: echo "Code is successfully pushed!!!"
